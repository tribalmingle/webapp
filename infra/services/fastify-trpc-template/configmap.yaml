apiVersion: v1
kind: ConfigMap
metadata:
  name: template-fastify-trpc-config
  namespace: platform
  labels:
    app: template-fastify-trpc
    component: api
    managed-by: gitops
  annotations:
    config.kubernetes.io/source: fastify-trpc-template
    tribalmingle.io/config-version: "1"
data:
  SERVICE_NAME: template-fastify-trpc
  SERVICE_VERSION: "0.1.0"
  UPSTREAM_GRAPHQL_URL: https://graphql.internal.svc.cluster.local/query
  REDIS_URL: redis://redis.platform.svc.cluster.local:6379
  FEATURE_FLAGS: "{}"
