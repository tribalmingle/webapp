apiVersion: v1
kind: Service
metadata:
  name: platform-gateway-canary
  namespace: platform
  labels:
    app: platform-gateway
spec:
  selector:
    app: platform-gateway
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
